{
  "provider_id": {
    "description": "主 LLM 提供商 (覆盖会话默认)",
    "type": "string",
    "_special": "select_provider",
    "hint": "留空则使用当前会话的默认模型。填入后本插件的所有 LLM 调用都会走这个提供商",
    "default": ""
  },
  "provider_mode_a": {
    "description": "模式 A (纯叙事) - 留空用上面的",
    "type": "string",
    "_special": "select_provider",
    "hint": "可选覆盖。模式 A 无工具调用,可选用更便宜/更快的模型",
    "default": ""
  },
  "provider_mode_b": {
    "description": "模式 B (RPG) - 留空用上面的",
    "type": "string",
    "_special": "select_provider",
    "hint": "可选覆盖。需要 function calling 能力,推荐用擅长工具调用的模型",
    "default": ""
  },
  "provider_mode_c": {
    "description": "模式 C (DND 跑团) - 留空用上面的",
    "type": "string",
    "_special": "select_provider",
    "hint": "可选覆盖。规则多 + 掷骰多,推荐能力强的模型",
    "default": ""
  },
  "image_compress_enable": {
    "description": "图片先压缩再发给模型(默认开)",
    "type": "bool",
    "hint": "/创建、/do 收到图片时,先等比缩放 + 重编码(JPEG/PNG),再传给 LLM。大幅降低多模态 token 消耗与上传耗时;压缩失败自动回退原图。动图(GIF)跳过压缩保持原样",
    "default": true
  },
  "image_max_size": {
    "description": "图片压缩后最长边(px)",
    "type": "int",
    "hint": "等比缩放的最长边像素上限,超过才缩放。默认 1280(与 AstrBot 内置压缩一致)。范围 64-8192",
    "default": 1280
  },
  "image_quality": {
    "description": "JPEG 压缩质量(1-100)",
    "type": "int",
    "hint": "重编码质量,越低文件越小。默认 90。透明图(PNG)不受此项影响",
    "default": 90
  },
  "img_desc_provider_id": {
    "description": "图片转述专用模型(主模型不支持多模态时用)",
    "type": "string",
    "_special": "select_provider",
    "hint": "当叙事主模型不支持图片输入(modalities 未勾选 image)时,/创建、/do 的图片会先经此模型转述为文字描述再注入剧情上下文。留空则使用 AstrBot 系统设置里的「默认图片转述模型」(provider_settings.default_image_caption_provider_id);两者都未配置时图片仅以占位提示告知模型",
    "default": ""
  },
  "img_desc_prompt": {
    "description": "图片转述提示词(可选)",
    "type": "string",
    "hint": "调用图片转述模型时使用的提示词。留空用内置提示词(侧重人物外观/服饰/场景细节,便于剧情延续)",
    "default": ""
  },
  "tool_max_steps": {
    "description": "模式 B/C 单次 LLM 调用最大工具步数",
    "type": "int",
    "hint": "tool_loop_agent 的 max_steps。范围 5-100。值过小可能回合未完成,过大响应极慢。默认 30",
    "default": 30
  },
  "tool_call_timeout": {
    "description": "单次工具调用超时 (秒)",
    "type": "int",
    "hint": "tool_loop_agent 的 tool_call_timeout。范围 10-300。默认 60",
    "default": 60
  },
  "max_history_chars": {
    "description": "叙事历史最大字符数",
    "type": "int",
    "hint": "超过此长度时,前面的消息会压缩为【叙事历史摘要】,仅保留最近 keep_tail_messages 条原文。默认 60000",
    "default": 60000
  },
  "keep_tail_messages": {
    "description": "压缩后保留的最近消息条数",
    "type": "int",
    "hint": "默认 20。建议保持偶数 (user + assistant 算一对)",
    "default": 20
  },
  "use_llm_compress": {
    "description": "历史压缩使用 LLM(默认开)",
    "type": "bool",
    "hint": "开启后,超长历史会调用 LLM 压缩成摘要(质量更好,稍慢);关闭则用纯规则抽取标题与世界观(快但粗)。LLM 调用失败会自动回退到规则抽取",
    "default": true
  },
  "record_llm_stats": {
    "description": "上报 LLM 用量到系统数据统计(默认开)",
    "type": "bool",
    "hint": "开启后,本插件的所有 LLM 调用(叙事生成/工具循环/模式识别/历史压缩)的 token 消耗与调用量会写入 AstrBot 全局统计,在 WebUI「数据统计」页可见",
    "default": true
  },
  "compress_provider_id": {
    "description": "压缩任务专用模型(可路由到便宜/快速的模型)",
    "type": "string",
    "_special": "select_provider",
    "hint": "可选。留空则用 provider_id(再留空用主 provider 或会话默认)。压缩只是抽取要点,可以路由到便宜模型而不必用主模型",
    "default": ""
  },
  "use_llm_mode_detect": {
    "description": "模式识别使用 LLM(更准确)",
    "type": "bool",
    "hint": "开启后,/创建 时会调 LLM 分析语境判断 A/B/C(默认开)。关闭则只用关键词匹配(快但简陋,可能误判)。LLM 调用失败自动回退关键词",
    "default": true
  },
  "mode_detect_provider_id": {
    "description": "模式识别专用模型(可路由到便宜的)",
    "type": "string",
    "_special": "select_provider",
    "hint": "可选。模式识别只是简单分类,可路由到便宜快速模型。留空用 provider_id(再留空用主 provider 或会话默认)",
    "default": ""
  },
  "output_as_image": {
    "description": "叙事输出转图片(默认关)",
    "type": "bool",
    "hint": "开启后 /创建、/do 的叙事输出自动渲染为图片(基于 pillowmd,无浏览器)。关闭则保持纯文本。转图失败自动回退纯文本",
    "default": false
  },
  "output_image_style_path": {
    "description": "pillowmd 模板样式目录(可选)",
    "type": "string",
    "hint": "指向 CustomMarkdownImage 风格的样式目录(含 setting.json + elements.json)。留空用 pillowmd 默认样式。样式 elements.json 带 page>0 的多帧动画背景时,输出自动变为 GIF 动图(如 独角兽gif 风格)。https://github.com/Monody-S/CustomMarkdownImage/tree/main/styles 可下载模板",
    "default": ""
  },
  "output_image_auto_page": {
    "description": "自动分页(内容长时按黄金分割比拆分)",
    "type": "bool",
    "hint": "渲染时启用 pillowmd 的 autoPage(尽可能接近黄金分割比,避免图片过长)。关闭后内容多时会生成超长单图。GIF 样式下自动分页仍输出动图",
    "default": true
  },
  "chat_card_enable": {
    "description": "剧情对白显示为聊天卡片(IM 样式,实验性)",
    "type": "bool",
    "hint": "开启后,剧情中的角色对白会以即时通讯气泡的形式渲染成图片(类似蓝色档案·桃信)。需先关闭本插件的「叙事输出转图片」或两者可同时开启,互不影响。对白识别依赖「角色名:内容」格式。",
    "default": false
  },
  "chat_card_theme": {
    "description": "聊天卡片主题(light/dark)",
    "type": "string",
    "hint": "light=米白底(默认), dark=深色底",
    "default": "light"
  },
  "chat_card_title": {
    "description": "聊天卡片标题(留空不显示标题栏)",
    "type": "string",
    "hint": "显示在聊天气泡顶部的标题,类似群聊名称。留空则不显示标题栏。",
    "default": ""
  },
  "chat_card_self_names": {
    "description": "兜底判定「主角」的称呼(逗号分隔,靠右蓝色气泡)",
    "type": "string",
    "hint": "推荐让 LLM 用 *名字: 台词 标记主角(见提示词说明)。这里作为兜底:对白说话人等于这些名称时也视为主角。多个用英文逗号分隔。",
    "default": "我,自己,你,玩家"
  },
  "chat_card_width": {
    "description": "聊天卡片宽度(px)",
    "type": "int",
    "hint": "整张卡片宽度,默认 1024。越大气泡越宽、每行字数越多。",
    "default": 1024
  },
  "chat_card_font_size": {
    "description": "正文字号(px)",
    "type": "int",
    "hint": "气泡内文字大小,默认 34。",
    "default": 34
  },
  "chat_card_max_pages": {
    "description": "聊天卡片最大分页数(超出放弃)",
    "type": "int",
    "hint": "对白过长时最多分几页(张图)。超过上限会回退为纯文本,避免消息过长刷屏。",
    "default": 5
  },
  "lore_selective_load": {
    "description": "选择性加载 lore(默认开,减少 system prompt 占用)",
    "type": "bool",
    "hint": "开启后:最近 N 轮出场过的角色完整注入设定,未出场角色只保留一行提示,需要刻画时由 LLM 调 life_sim_get_character_lore 按需读取。世界观设定条目通常不多,**始终完整注入**(不做按需裁剪,避免模型不读取历史就凭印象写)。角色多/轮数多时显著省 token。关闭则角色也全部完整注入(旧行为)",
    "default": true
  },
  "lore_active_rounds": {
    "description": "活跃角色检测窗口(轮)",
    "type": "int",
    "hint": "最近 N 轮对话中出场过的角色视为活跃角色,完整注入其设定;窗口外的角色只显示名字,需要时由 LLM 按需读取。默认 6",
    "default": 6
  }
}
