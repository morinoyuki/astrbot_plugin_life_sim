# Changelog

本文件记录自 v1.0.0 以来的全部改动，版本与仓库语义化版本保持一致。

## [1.1.0] - 2026-09-02

### ✨ 新增功能

- **聊天卡片与角色头像系统**：新增 im_render 渲染引擎，支持模型自选头像，聊天以卡片/气泡形式呈现，完善元数据。
- **聊天卡片美化**：整体样式美化、彩色 emoji 支持、渲染提速与高度修复。
- **头像系统**：
  - `/头像` 支持引用图片设置（当前消息无图时自动回退引用消息图片）。
  - `/头像 查看` 列出并展示角色头像图片。
  - 头像按会话 scope（群/私聊）分区存储，并同步清理。
- **向量记忆系统**：
  - 全自动记录 + 独立 id 管理工具 + LLM 总结重构。
  - 记忆写入重构（精简摘要 + 主动分工）、标签转义。
  - 回忆注入改用 XML 标签并隔离下游，lore 仅存设定/规则。
  - 聊天卡片新增「选项 / 状态」标签样式。
  - 后台页新增向量记忆管理入口。
- **剧情历史重构**：
  - 单文件存储：每个 scope 一个 `history.json` + 快照版本表去重，旧布局自动迁移。
  - 分支历史与 `history.json` 同目录存放、按当前分支字段选择，切换零复制，老会话 `'主线'` 自动归一为 `'主线'` 并触发旧多文件迁移。
  - 剧情/快照存续去重：lore/RPG 快照收敛为内容寻址版本表。
  - `/undo N` 按「轮」精确回滚剧情历史，并同步回滚记忆。
- **WebUI**：新增插件页面「数据管理」，可查看/编辑/清理全部存档。
- **图片处理**：`/创建`、`/do` 图片先压缩再交给 LLM；主模型无多模态时自动转述。
- **Lore**：`/lore` 支持删除 `<角色名>`。
- **统计上报**：LLM 用量上报到 AstrBot 系统数据统计。

### 🎨 聊天卡片 / 渲染细节

- 对白识别改为结构化标签 `<d>/<c>`，移除冒号启发式解析。
- 无口/不善言语角色的笔谈也视为对白，用 `【】` 气泡呈现。
- 要求 LLM 自行核对头像名单，名字有出入时用 `@` 显式选头像。
- 增强 prompt 约束：禁止动作/神态混入角色名、禁止灰胶囊内混入台词。
- 对白解析修复：角色名混入动作/神态时判为旁白而非名字气泡。
- 聊天卡片模式下加「覆盖声明」，并将排版段拆分为聊天版专用常量（取代覆盖声明）。
- `#/##/###` 标题字体改用仓耳小丸子等标题专用字体。

### 🐛 修复

- 聊天卡片高度计算漏算行间距导致底部内容被裁。
- emoji 显示方块：参考 pillowmd secondFonts 强化字体回退，探测系统彩色 emoji 字体并扩展备用字体清单。
- emoji 回退覆盖灰胶囊/标题/测量等所有绘制路径（逐字符回退）。
- 行动选项卡超长截断不再越界（省略号在卡片内）。
- `/lore` 输出超长导致平台拒发——用户展示路径截断，LLM 注入保持全文。
- 查看头像时不应删除已存储的头像文件。
- 清理已删除工具 `life_sim_get_world_lore` 的残留引用。

### ♻️ 重构 / 优化

- `keep_tail_messages` 单位改为「轮」（以用户输入计），并使用 ruff（lint + format）全面整理项目。
- 移除剧情分支相关说明，简化行动选项描述。

### 📦 工程化 / 其他

- 测试文件纳入版本管理（新增多个针对分支、记忆、快照去重、undo/redo、卡片样式、历史压缩等测试）。
- 新增 `tools/migrate_snapshots.py` 迁移脚本。
- `.gitignore` 添加 `ruff_cache` 目录。
- 更新版本号至 1.1.0。

---

[1.1.0]: 自 commit `fde1c36`（v1.0.0）以来的全部改动。
